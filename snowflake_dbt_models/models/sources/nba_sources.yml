version: 2

sources:
  - name: snowflake_db_nba_data
    database: player_data
    schema: player_master_data
    tables:
      - name: player_list
        columns:
          - name: id
            tests:
              - not_null
      - name: player_details
        columns:
          - name: id
            tests:
              - not_null
          
    database: reports
    
